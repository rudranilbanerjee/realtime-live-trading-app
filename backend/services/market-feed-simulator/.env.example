# Market Feed Simulator
NODE_ENV=development

# WebSocket URL of market-data-service (where simulated ticks are sent)
MARKET_DATA_WS_URL=ws://localhost:4001

# How often (ms) to generate a new price tick
TICK_INTERVAL_MS=1000
