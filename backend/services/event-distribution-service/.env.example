# Event Distribution Service (custom event broker)
NODE_ENV=development
PORT=3003

# How many recent events to keep in memory per topic (replayed to reconnecting consumers)
EVENT_BUFFER_SIZE=100

# How often (ms) to ping consumers to detect dead connections
HEARTBEAT_INTERVAL_MS=10000
